---
Created: 2021-01-15T20:25:00
URL 1: ""
Tags: [topic/技術/ソフトウェア設計]
---
### ループするやつ

0 → 1 → 2 → 0 → 1 → 2 → …

```plain text
x += 1
if(x == length) x = 0
```

↓

```plain text
x = (x + 1) % length
```

1 → 2 → 3 → 1 → 2 → 3 → …

```plain text
x += 1
if(x > length) x = 1
```

↓

```plain text
x = x % length + 1
```

2 → 1 → 0 → 2 → 1 → 0 → …

```plain text
if(x == 0) x = length
x -= 1
```

↓

```plain text
x = (x - 1 + length) % length
```

下記みたいな処理はJavaScriptでよくやります。

```plain text
next_index = (current_index + 1) % length;
prev_index = (current_index - 1 + length) % length;
```

### 角度

0 <= angle < 360 に収める処理

```plain text
if(angle < 0) angle += 360
if(angle >= 360) angle -= 360
```

↓

```plain text
angle = (angle + 360) % 360
```

### ある条件を満たす場合に関数を実行

```plain text
if(is_valid()) fn()
```

↓

```plain text
is_valid() && fn()
```

上記は特に読みやすくもなっていないように見えますが、文で書かれているものを式で表現することができるという利点があります。つまり、式を評価した値をそのまま使う(変数に代入したり、別の関数の引数にしたりする)ことができるようになります。(※言語によります。)

JavaScriptではデフォルト引数を設定するイディオムに、下記のようなものがあります。(※下記コードには、真偽判定による問題点があります。[引数を渡さなかった際にデフォルト値を使う場合の注意点](http://qiita.com/VoQn/items/3a4a8c0ad11e45c64907)を参照。)

```plain text
var f = function(arg){
  arg = arg || 1;
  // ...
}

```

### 条件分岐を隠蔽する(PHP)

これまでの例とはちょっと違いますが、頻出する条件分岐は抽象化してしまった方が読みやすくなるケースがあります。

```plain text
$x = isset($array["key"]) ? $array["key"] : null;
```

↓

```plain text
function elem($array, $key, $default=null){
  return isset($array[$key])) ? $array[$key] : $default;
}

$x = elem($array, "key");
```

ここまでの例・考え方を適用することで、全てが必ずしも最適化されたり読みやすくなる訳ではありません。が、手段を複数用意して適材適所で選択できるようになることは、意義のあることだと思います。

![](https://neco.tech/blog/wp/wp-content/plugins/wp-social-bookmarking-light/public/images/facebook.png)

![](https://b.hatena.ne.jp/images/entry-button/button-only@2x.png)

[Pocket](https://getpocket.com/save)