# New database

filters:
  and:
    - note["base"] == link("New database.base")
properties:
  file.name:
    displayName: (p)pick
  コミットをそのまま残す。:
    displayName: コミットをそのまま残す。
views:
  - type: table
    name: Table View
    order:
      - file.name
      - コミットをそのまま残す。
