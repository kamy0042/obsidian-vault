# New database

filters:
  and:
    - note["base"] == link("New database.base")
properties:
  file.name:
    displayName: pending
  実行中 Promise.resolve()が呼ばれる前:
    displayName: 実行中 Promise.resolve()が呼ばれる前
views:
  - type: table
    name: Table View
    order:
      - file.name
      - 実行中 Promise.resolve()が呼ばれる前
