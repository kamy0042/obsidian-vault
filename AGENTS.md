# LLM Wiki スキーマ（AGENTS.md）

この Vault で LLM が構築・保守する永続的な知識 wiki の運用規約。[Karpathy の LLM Wiki パターン](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)をこの Vault に適応させたもの。

> 知識は一度コンパイルされ、以後は現在に保たれる。質問のたびに生ソースから再導出しない。

RAG との違い: クエリ時に毎回生ドキュメントを検索するのではなく、LLM が永続的な wiki を段階的に構築・保守する。人間の仕事はソースの厳選・質問の設定・意味の検討。LLM の仕事は読み書きと簿記（相互参照の更新、矛盾検出、一貫性維持）のすべて。

**応答・生成するページはすべて日本語**（固有名詞は原語のまま）。

このファイルはスキーマ層であり、Vault 外のセッションからは `_obsidian-wiki` skill 経由で参照される。

## 3層アーキテクチャとパス

```bash
VAULT="/Users/kamy0042/workspace/obsidian-vault"
WIKI="$VAULT/Wiki"
```

| 層          | 実体                              | 扱い                                                  |
| ----------- | --------------------------------- | ----------------------------------------------------- |
| Raw Sources | `Articles/` `Clippings/` `Notes/` | **原則読み取り専用。操作4の限定例外を除き編集しない** |
| Wiki        | `Wiki/`                           | 生成・編集・保守すべて可                              |
| Schema      | この AGENTS.md                    | wiki 構造・規約・ワークフローの定義                   |

上記以外のフォルダ（`Attachments/` `Archive/` `Bases/`）は wiki システムの対象外。参照も書き込みもしない。

**スライド系クリップは無視する**: ファイル名に `Speaker Deck` / `SlideShare` を含むクリップ（約70件）は、トランスクリプトの欠落・文字化けが多く信頼できるソースにならないため、ingest・query の対象から除外する。タグ検索やGrepの結果に混ざっても読まない。

**タグ検索は両形式対応パターンで行う**: `Articles/` のタグ記法は2形式が混在する（旧 `Tags: [...]` インライン配列 / 新 `tags:` + `- タグ` リスト形式 = 2026-07-10以降のWeb Clipper新テンプレート。Obsidian上は同一プロパティ）。片方だけのパターンでは新旧どちらかを取りこぼす。

```bash
# topic で絞る + スライド系除外（両形式対応）
grep -lE '^(Tags:.*|\s*- )topic/デザインシステム' "$VAULT/Articles/"*.md | grep -viE 'speaker deck|slideshare'
```

```
Wiki/
├── index.md      — 全 wiki ページの索引（ingest のたびに更新。summary は除く）
├── log.md        — 記事・wiki の追記専用リリースノート
├── entity/       — type: entity のページ
├── concept/      — type: concept のページ
├── summary/      — type: summary のページ（全記事の要約索引層、遅延生成。主トピック別サブフォルダ）
└── note/         — type: note のページ
```

ページは `Wiki/<type>/` に置き、サブフォルダと frontmatter の `type` を常に一致させる。これは Obsidian ファイルエクスプローラーの見通しのための物理配置であり、**意味的な分類は従来どおり wikilink と index.md が担う**。type 以外の軸（topic 等）でのサブフォルダは作らない（トピック横断ページに単一の親を強制し、Articles のタグ体系と二重管理になるため。例外は `summary/` 配下の主トピック別サブフォルダのみ——詳細は「summary 層」参照）。summary/ と note/ は最初のページを作る時にフォルダごと作成する（空フォルダを置かない）。wikilink はフォルダ横断で解決されるため、ページ移動で既存リンクは壊れない。

Raw Sources の構造・タグ体系・検索方法の詳細は kintone-Design-System リポジトリの `_obsidian-ref` skill を参照（`Articles/` は frontmatter の `topic/` タグで分類。記法は新旧2形式が混在するため、検索は後述の両形式対応パターンを使う）。

## summary 層（記事要約の索引）

`Wiki/summary/` は記事1件につき1ページの要約を置く索引層。知識層（entity / concept）が「深く狭い」のに対し、summary 層は「浅く広い」。知識の圧縮ではなく索引なので、密度原則には反しない（圧縮は entity / concept への編纂で行う）。

- **ファイル名は `<記事ファイル名> 要約.md`**。記事と同名にすると `[[wikilink]]` の解決が曖昧になるため、必ず「要約」サフィックスを付ける
- **配置は主トピック別サブフォルダ** `Wiki/summary/<主トピック>/<第2階層>/`。主トピック = 記事の**先頭 topic/ タグの第1階層**（`topic/技術/React` → `技術`、`topic/AI` → `AI`）。先頭タグに第2階層があればさらにサブフォルダを掘る（`topic/デザインシステム/導入事例` → `デザインシステム/導入事例/`）。第2階層が無い記事は `<主トピック>/` 直下、topic タグが無い記事は `未分類/`。知識層のトピックサブフォルダ禁止の例外だが、summary ページは記事と1:1で親が機械的に決まるため単一親問題は起きない（複数トピック記事は先頭タグ優先の割り切り）。summary ページの参照・存在確認は常にサブフォルダ横断の `find` で行う。**タグ再編（第2階層のリネーム・統合）時は summary ページのフォルダ移動で追従する**
- 標準は4〜5文。導入部の紹介で終わらせず、記事の主張・結論まで含める
- **本文は1文ごとに改行する（句点改行）**。要約に列挙が自然に登場する場合のみビュレットリストとして書く（無理なリスト化はしない）。Obsidian は単一改行を表示改行として描画するため、句点改行がそのまま読みやすさに効く
- 長大・重要なソースは同じページを深く育ててよい（別ページは作らない）
- **遅延生成**: 一括では作らない。どの操作（ingest / query / 要約確認）であれ、**記事の本文を全文読んだら必ず対応する summary ページを作成・更新する**。使うほど索引が育つ
- index.md には個別掲載しない。記事との対応はファイル名規約で機械的に辿れる
- **目次は各フォルダ内の `00_index.md`**（ファイル名自然言語規則の例外）。第2階層フォルダの 00_index は配下の要約ページのフラットな一覧（type 見出しは付けない）、主トピック直下の 00_index は第2階層フォルダへのリンクと件数の一覧。要約ページの増減・タグ再編に合わせて機械的に再生成する
- タグは持たせない。トピックでの絞り込みは常に Articles 側のタグ検索で行う

ページ本文の形:

```markdown
---
type: summary
updated: YYYY-MM-DD
---

# <記事名> 要約

ソース: [[記事ファイル名]]

4〜5文の要約。
1文ごとに改行する。
列挙が自然に登場する場合のみリストにする。
```

### 探索ファネル（記事の特定）

対象記事が明示されず、トピックや観点で指定されたときは、安い順に絞り込む:

1. **タグで候補を絞る** — 両形式対応 grep（前述）。タグ付けが粗い記事の取りこぼしが疑われる場合はキーワード grep で補完
2. **summary ページと照合** — 候補記事に対応する `<記事名> 要約.md` を `find "$WIKI/summary"` で探し、あれば読んで関連性を判定
3. **summary が無い候補は冒頭をスキム** — `head -30` 程度（タイトル + 導入部）で選別
4. **選抜した記事のみ全文を読む** — 読んだら summary ページを作成

## ページタイプと命名

| type      | 用途                                                                 | 例                                 |
| --------- | -------------------------------------------------------------------- | ---------------------------------- |
| `entity`  | 人物・組織・プロダクト・仕様など、他所からリンクされうる独立した実体 | `Brad Frost.md` `DTCG.md`          |
| `concept` | テーマ・考え方・複数ソースを横断する論点                             | `デザイントークンの命名戦略.md`    |
| `summary` | 単一ソースの要約。全記事を対象に遅延生成する索引層（前述）           | `Design Tokens 101 要約.md`        |
| `note`    | query への良い回答を保存したもの                                     | `Q. トークン階層は何段が適切か.md` |

- **ファイル名 = ページタイトル = 自然言語**。この Vault は日本語ファイル名主体なので、kebab-case スラッグは使わない。人名・固有名詞は原語のまま
- ページ間参照はすべて `[[wikilink]]`。Raw Sources への参照も `[[記事ファイル名]]` で張る（Obsidian はフォルダ横断で解決し、グラフビューに乗る）

### frontmatter スキーマ

```yaml
---
type: entity | concept | summary | note
entity-type: person | organization | product | spec # entity のみ
aliases: [別名, 英語名] # 任意
updated: YYYY-MM-DD
---
```

**`Tags:` プロパティは使わない**。`Tags:` は Articles のトピック分類体系（Bases のビューが依存）であり、wiki ページが混入すると汚染になる。wiki の分類は wikilink と index.md で行う（それが Memex の思想でもある）。

### ページ本文の構造（entity の例）

```markdown
# Brad Frost

## 概要

Atomic Design の提唱者。…

## 主要な事実

- Atomic Design を2013年に提唱（出典: [[記事A]]）
- …（出典: [[記事B]], https://example.com）

## 登場するソース

[[記事A]], [[記事B]]

## 関連ページ

[[Atomic Design]], [[コンポーネント設計]]
```

事実には必ず出典（`[[ソース]]` または URL）を添える。出典のない主張は書かない。

## 新規ページ vs 既存ページ編集の判断

- **他所からリンクしたくなる独立した実体・概念** → 新規ページ
- **既存の実体の属性・事実の追加/更新** → 既存ページを編集
- 迷ったら既存ページに書き、lint 時に分離を検討する

**密度を優先する**: 圧縮が効くのは**複数ソースに散在した事実を1つの高密度ページに統合したとき**。summary ページは索引層であって知識の圧縮ではないため、summary を書いただけで編纂を終えない。知識として重要な事実は entity / concept ページへ編纂する。

## 操作1: Ingest（取り込み）

対象: Vault 内の記事（`Articles/` `Clippings/` `Notes/`）、URL（WebFetch で取得）、会話中のテキスト。対象記事が明示されていない場合は探索ファネル（前述）で候補を特定する。

1. ソースを読む。URL の場合は WebFetch で取得し、原文を Vault に残したければ Web Clipper でのクリップをユーザーに提案（原文保存は Raw 層の仕事。wiki が代行しない）
2. 主要な主張・事実・登場する実体を抽出し、**ユーザーに要点を提示**（何を wiki に反映するかの認識合わせ。明確な指示が既にあればスキップ可）
3. 影響するページを洗い出す: 既存ページは Grep で `$WIKI` を検索して特定。1ソースが10〜15ページに触れることもある
4. ページを作成・更新（上記の判断基準に従う）
5. `index.md` を更新
6. 記事または wiki の実質的な内容変更があれば `log.md` に追記

**一括 ingest はしない**: Articles には1,000件超の記事があるが、まとめて取り込まない。ingest はユーザーが指定したソース単位・オンデマンドで行う。

## 操作2: Query（質問）

1. `index.md` を読み、関連ページを特定（足りなければ Grep で `$WIKI` を全文検索）
2. 該当ページを読み、**引用付き**で回答を合成（`[[wikiページ]]` と元ソースの両方を示す）
3. wiki に情報がない・薄い場合: 探索ファネル（タグ → summary ページ → 冒頭スキム → 選抜記事の全文）で候補記事を集め、複数記事を横断した回答を合成する。全文を読んだ記事は summary ページを作成し、「この内容を ingest するか」を提案（query の失敗は ingest の入口）
4. 回答が再利用価値のある合成になった場合、`note` ページとしての保存を提案
5. summary / note / entity / concept に実質的な内容変更が生じた場合だけ `log.md` に追記する。回答しただけなら記録しない

## 操作3: 要約確認（記事の要約提示）

ユーザーが特定記事（または直近クリップ群）の要約を求めたときの操作。

1. 対応する `<記事名> 要約.md` を `find "$WIKI/summary"` で探し、あればそれを基に即答
2. なければ記事本文を全文読み、summary ページを作成 → 提示
3. summary を作成・更新した場合だけ `log.md` に追記する。既存要約を提示しただけなら記録しない

## 操作4: 新規クリップ処理

ユーザーから「新たに追加した記事を処理して」などの**明示的な指示があった場合だけ**実行する。自動監視・定期実行・指示のない一括処理はしない。対象は `Clippings/` に新たに追加された記事とし、複数ある場合はユーザーが指定した記事、または指示から特定できる直近の記事だけを処理する。

1. 記事の本文を全文読み、主要な主張・事実・登場する実体を確認する
2. Articles の既存タグ体系と類似記事を確認し、記事の frontmatter に適切な `topic/` タグを設定する。最も関連するタグを必ず先頭に置き、既存の非 topic タグや他の frontmatter は保持する
3. `<記事ファイル名> 要約.md` を summary 層の規約に従って作成する。配置には手順2で確定した先頭 `topic/` タグを使う
4. Ingest の手順に従い、重要な知識を entity / concept ページへ編纂し、`index.md` を更新する
5. すべての処理が成功したことを確認してから、記事ファイルを `Clippings/` から `Articles/` へ移動する。同名ファイルが既に存在する場合は上書きせず、移動を中止してユーザーに報告する
6. 成功後、追加した記事と wiki の実質的な内容変更を `log.md` に追記する

手順の途中で失敗した場合は後続処理を止め、記事を `Clippings/` に残す。summary のソースリンクはファイル名だけの wikilink とし、移動後も解決できる形を保つ。

## 操作5: Lint（健全性チェック）

定期的（目安: ingest 5〜10回ごと、またはユーザー依頼時）に wiki を検査する。**最大の失敗モードはドリフト**（相互参照の未更新）。

チェック項目:

- **赤リンク**: 存在しないページへの `[[...]]` → ページ作成候補 or リンク修正
- **オーファン**: どこからもリンクされていないページ（`summary/` は索引層なので対象外）
- **index 乖離**: index.md に無い実ページ / 実体の無い index 項目（`summary/` は個別掲載しないため対象外）
- **type とフォルダの不一致**: `Wiki/<type>/` 配下のページの frontmatter `type` がフォルダ名と食い違う
- **矛盾**: ページ間で食い違う主張 → 新しいソースを優先して更新
- **未ページ化の頻出概念**: 本文で繰り返し言及されるがページがないもの
- **鮮度**: `updated` が古いのに関連ソースが増えているページ
- **summary 整合**: ソース記事が実在しない summary ページがないか（記事のリネーム・削除の検出）

機械チェックの例:

```bash
# wiki 内の全 wikilink を列挙
grep -rhoE '\[\[[^]|]+' "$WIKI" --include='*.md' | sed 's/\[\[//' | sort -u
# 実ページ一覧（wikilink はフォルダ横断で解決されるため Raw Sources も含める）
find "$WIKI" "$VAULT/Articles" "$VAULT/Clippings" "$VAULT/Notes" -name '*.md' -exec basename {} .md \; | sort -u
# type とフォルダの一致（不一致ファイルを列挙。出力なしが正常。summary/ はサブフォルダがあるため find で再帰）
for t in entity concept summary note; do [ -d "$WIKI/$t" ] && find "$WIKI/$t" -name '*.md' -exec grep -L "^type: $t" {} +; done
```

両者を突き合わせて赤リンク・オーファンを検出。矛盾・鮮度は該当ページを読んで判断する。結果はレポートとして提示し、修正は承認を得てから適用。修正によって wiki の実質的な内容が変わった場合だけ `log.md` に追記し、検査結果だけなら記録しない。

## index.md フォーマット

```markdown
# Wiki Index

## Entities

- [[Brad Frost]] — Atomic Design 提唱者 | sources: 3

## Concepts

- [[デザイントークンの命名戦略]] — 階層設計と命名規則の論点整理

## Notes

- [[Q. トークン階層は何段が適切か]] — 2026-07-10
```

1ページ1行、一行説明付き。summary ページは掲載しない（`<記事名> 要約` のファイル名規約で機械的に辿れるため）。wiki が数百ページを超えて全文走査が苦しくなったら、ハイブリッド検索（SQLite FTS5 + 埋め込み）への移行を検討する（それまでは index 走査で十分）。

## log.md フォーマット（リリースノート・追記専用）

`log.md` は作業日誌ではなく、この Vault で新たに読めるようになった記事・知識と、その内容変更を伝えるリリースノートとする。内部作業ではなく、読者から見た変化を書く。

- リリース見出しは `## YYYY-MM-DD — <変更内容を表す短いテーマ>` とする。バージョン番号や `ingest` `query` `maintenance` などの作業種別は付けない
- 本文は `### 追加` `### 更新` `### 削除` の順とし、該当項目がない見出しは省略する。改名は `更新` に含め、旧名から新名への変更を示す
- 各項目は1記事または1ページにつき1行とし、先頭に `**記事**` `**Wiki**` `**要約**` の種別を付ける
- 説明は「何をしたか」ではなく、**何が読めるようになったか・どの知識が変わったか**を一文で示す
- 追加記事には対応する summary へのリンクを添える。同時に作った summary は別項目として重複掲載しない。既存記事に新しく summary だけを作った場合は `**要約**` として記す
- 既存記事の summary を一括生成した場合は、個別ページを列挙せず、対応する `00_index.md` への「要約コレクション」1項目にまとめてよい
- 記録しないもの: 調査・検索・コマンド・バッチ分割・タグ設定・ファイル移動などの手順、件数集計、失敗や保留、`index.md` / `00_index.md` の機械的更新
- ファイルに実質的な内容変更がない query・要約確認・lint は記録しない。失敗や保留はその場の応答で伝える
- 既存エントリは、ユーザーから明示的な再編依頼がない限り遡って書き換えない

```markdown
## 2026-09-22 — AIネイティブなデザインシステム

### 追加

- **記事** [[AIネイティブを目指した「これまで」と「これから」｜agu relay（リレイ）]] — 狩野モデルで責任範囲を分け、MCP・hooks・evalsへ接続する実践（[[AIネイティブを目指した「これまで」と「これから」｜agu relay（リレイ） 要約|要約]]）
- **Wiki** [[relay Design System]] — relay の構成、責任分担、AI活用を整理

### 更新

- **Wiki** [[AI時代のデザインシステム]] — MCP・hooks・evalsを組み合わせる実践を追加
```

## ガードレール

- **Raw Sources は原則不可侵**: `Articles/` `Clippings/` `Notes/` への書き込み・編集は、操作4における対象記事のタグ編集と `Clippings/` から `Articles/` への移動だけを例外とする
- **wiki の書き込みは原則 `Wiki/` 内に限定**（操作4の限定例外を除き、Vault 内の他フォルダには書き込まない）
- `log.md` には Articles への追加と Wiki の実質的な内容変更だけを記録し、作業だけの履歴は残さない
- 事実には必ず出典を添える。出典を示せない推測は書かない
- 日付は `date +%Y-%m-%d` で取得する（推測しない）
- ファイル名の全角文字・スペースがあるため、Bash でのパス指定は常にダブルクォートで囲む
